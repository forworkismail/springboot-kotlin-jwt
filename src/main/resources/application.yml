spring:
  jpa:
    database: POSTGRESQL
    show-sql: true
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
      #format_sql: true
      dialect: org.hibernate.dialect.PostgreSQLDialect
  datasource:
    url: jdbc:postgresql://localhost:5432/jwt_auth_db
    username: jwt_auth_user
    password: jwt_auth_password
    driverClassName: org.postgresql.Driver
  sql:
    init:
      platform: jwt_auth_db
server:
  servlet:
    contextPath: /api
jwt:
  secret: "iofwjeov0w9erg094tqj423ur;23dszv3gt8m48m45q890erqa5"